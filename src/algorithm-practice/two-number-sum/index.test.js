import {it, expect, describe} from 'vitest';

import {sumOfTwoNumber} from './'

describe('sum()', () => {
  it('should return the addition of the two number given', () => {

    expect(6).toBe(6)
  })
})